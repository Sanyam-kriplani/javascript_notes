//reduce function in javascript is an iterative method that iterate over an array and reduce  it to a single value.
//reduce method takes a reducer callback function and initial value as arguments
//reducer function take two values as arguments i.e. accumulator and current value
//accumulator holds either the initial value or the result of the previous iteration.

let Numbers=[1,2,3,4,5]

// let mytotal=Numbers.reduce((acc,curr)=>{
//     console.log(`acc:${acc} and the curr:${curr}`);
//     return acc+curr},0)
//     console.log(mytotal);